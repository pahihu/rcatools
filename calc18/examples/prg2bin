#
# compile prg to bin
#
if [ $# -ne 1 ];
then
   echo "usage: prg2bin file"
   exit 1
fi
../calc18 <$1.prg >out.$$
cat prolog out.$$ epilog >$1.sr
a18 $1.sr -r -L $1.lst -b $1.bin

ls -l $1.bin
rm -f out.$$
